@ $mol_replace
class $mol_number extends $mol.$mol_number {
	
	// @ $jin2_grab
	// presses() {
	// 	return new $jin2_atom<KeyboardEvent>( null , event => {
	// 		var field = <HTMLInputElement>event.target
	// 		if( event.keyCode === 13 ) {
	// 			this.changes().set( event )
	// 		}
	// 	} )
	// }
	
	// @ $jin2_grab
	// changes() {
	// 	return new $jin2_atom<Event>( null , event => {
	// 		var field = <HTMLInputElement>event.target
	// 		var val = field.value.trim()
	// 		if( val ) {
	// 			this.value().set( field.value )
	// 			field.value = String( this.value().get() )
	// 			setTimeout( () => field.focus() )
	// 		}
	// 	} )
	// }
	
}